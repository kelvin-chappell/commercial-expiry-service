machine:
  java:
    version: oraclejdk8
general:
  artifacts:
    - "~/commercial-expiry-service/target/riffraff/artifacts.zip"
dependencies:
  override:
    - pwd
test:
  override:
    - sbt test
  post:
    - sbt riffRaffUpload
