# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET        /management/healthcheck  controllers.Application.healthCheck()
GET        /item/*id                controllers.Application.adHocStream(id, expired: Boolean)
